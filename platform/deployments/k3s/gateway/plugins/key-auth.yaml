apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-key-auth
  namespace: kong-system
spec:
  plugin: key-auth
  config:
    key_names:
      - apikey
    hide_credentials: true
