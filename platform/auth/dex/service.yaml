apiVersion: v1
kind: Service
metadata:
  name: dex
  namespace: auth-system
  labels:
    app: dex
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 5556
      targetPort: 5556
      protocol: TCP
  selector:
    app: dex
