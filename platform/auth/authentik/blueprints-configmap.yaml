apiVersion: v1
kind: ConfigMap
metadata:
  name: authentik-blueprints
  namespace: auth-system
data:
  initial-setup.yaml: |
    version: 1
    metadata:
      name: Initial Platform Setup
    entries:
      # Create admin group with superuser privileges
      - model: authentik_core.group
        identifiers:
          name: admins
        attrs:
          is_superuser: true
      
      # Create engineering group with standard privileges
      - model: authentik_core.group
        identifiers:
          name: engineering
        attrs:
          is_superuser: false
      
      # Create admin user
      - model: authentik_core.user
        identifiers:
          username: admin
        id: admin-user
        attrs:
          name: Platform Administrator
          email: admin@platform.local
          is_active: true
          groups:
            - !Find [authentik_core.group, [name, admins]]
      
      # Create OIDC provider for Dex (structure only, no client secret)
      # Client secret will be set by Config Sync Job after Authentik is ready
      - model: authentik_providers_oauth2.oauth2provider
        identifiers:
          name: Dex OIDC Provider
        id: dex-provider
        attrs:
          authorization_flow: !Find [authentik_flows.flow, [slug, default-authentication-flow]]
          client_type: confidential
          client_id: dex-client
          redirect_uris: |
            https://dex.home.local/callback
          signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
          property_mappings:
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
      
      # Create application for platform services
      - model: authentik_core.application
        identifiers:
          slug: platform-services
        attrs:
          name: Platform Services
          provider: !KeyOf dex-provider
