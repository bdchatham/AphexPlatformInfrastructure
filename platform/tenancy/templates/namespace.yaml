apiVersion: v1
kind: Namespace
metadata:
  name: {{TENANT_NAME}}
  labels:
    arbiter.io/tenant: "{{TENANT_NAME}}"
    arbiter.io/managed-by: "platform-controller"
    arbiter.io/repo-org: "{{REPO_ORG}}"
    arbiter.io/repo-name: "{{REPO_NAME}}"
