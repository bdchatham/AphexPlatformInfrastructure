apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Tekton Pipelines v1.9.0
  - https://github.com/tektoncd/pipeline/releases/download/v1.9.0/release.yaml
  # Tekton Triggers v0.35.0
  - https://infra.tekton.dev/tekton-releases/triggers/previous/v0.35.0/release.yaml
  # Tekton Triggers Core Interceptors v0.35.0
  - https://infra.tekton.dev/tekton-releases/triggers/previous/v0.35.0/interceptors.yaml
  # Tekton Dashboard v0.65.0
  - https://github.com/tektoncd/dashboard/releases/download/v0.65.0/release.yaml

patchesStrategicMerge:
  - config-logging-triggers.yaml
