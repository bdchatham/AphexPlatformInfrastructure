---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: organizations.aphex.io
spec:
  group: aphex.io
  names:
    kind: Organization
    listKind: OrganizationList
    plural: organizations
    singular: organization
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: Display Name
      type: string
    - jsonPath: .status.namespace
      name: Namespace
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Organization is the Schema for the organizations API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OrganizationSpec defines the desired state of Organization
            properties:
              adminUsers:
                description: AdminUsers is a list of admin email addresses
                items:
                  type: string
                type: array
              displayName:
                description: DisplayName is the human-readable organization name
                type: string
              webhookSecret:
                description: WebhookSecret is the GitHub webhook secret (auto-generated
                  if not provided)
                type: string
            required:
            - adminUsers
            - displayName
            type: object
          status:
            description: OrganizationStatus defines the observed state of Organization
            properties:
              message:
                description: Message provides human-readable status information
                type: string
              namespace:
                description: Namespace is the organization namespace name
                type: string
              phase:
                description: Phase represents the organization provisioning status
                type: string
              webhookURL:
                description: WebhookURL is the external webhook URL for GitHub integration
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
